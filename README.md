# Просмотрщик карт .mbtiles

Локальный веб-просмотрщик набора растровых `.mbtiles` из `D:\карты`.
Панорамирование, плавный зум, работа с телефона по локальной сети.

## Запуск

```powershell
cd "D:\карты\viewer"
npm start
```

Затем откройте адрес, который сервер напечатает в консоли — обычно
<http://localhost:8080/>.

Если проект разворачивается с нуля — см. [INSTALL.md](INSTALL.md).
Для компьютера **без интернета** собирается автономный комплект, где уже
лежит портативный `node.exe` и все зависимости: ставить и качать там ничего
не нужно — см. раздел 0 в INSTALL.md.

## Управление

| Действие | Как |
|---|---|
| Панорамирование | перетаскивание мышью или пальцем (с инерцией) |
| Зум | колесо, `+` / `−`, двойной клик (с `Shift` — отдалить) |
| Зум на телефоне | щипок двумя пальцами |
| Сдвиг с клавиатуры | стрелки |
| Показать всё | кнопка с рамкой на правом борту |
| Выбрать уровень детализации | тянуть лестницу зумов между `+` и `−` |

Лестница справа — это шкала уровней пирамиды, а не просто ползунок.
Сплошные штрихи отмечают зумы, для которых в файлах есть настоящие тайлы;
полые сверху — зону за `maxzoom`, где картинка только растягивается.
Границу иначе не видно, а знать про неё важно: за ней подробностей
не прибавится, сколько ни приближай.

Текущий вид пишется в адресную строку (`#зум/широта/долгота`), поэтому
ссылку можно сохранить или переслать — карта откроется в том же месте.
Добавьте `?debug` к адресу, чтобы увидеть сетку тайлов и их номера.

## Объекты на карте

Второй блок кнопок на правом борту: метки, линии и полигоны поверх снимка.

| Действие | Как |
|---|---|
| Поставить метку | кнопка с булавкой или клавиша `M`, затем клик по карте |
| Нарисовать линию | кнопка с ломаной или `L`, клики по вершинам |
| Нарисовать полигон | кнопка с пятиугольником или `P`, клики по вершинам |
| Завершить фигуру | кнопка «Готово», двойной клик или `Enter` |
| Отменить рисование | кнопка «Отмена» или `Esc` |
| Выбрать объект | кнопка со стрелкой или `S`, затем клик по объекту |
| Переименовать | кнопка «Переименовать» или `F2` |
| Удалить | кнопка «Удалить» или `Delete` |
| Сохранить в файл | кнопка со стрелкой вниз — выгружает GeoJSON |
| Загрузить из файла | кнопка со стрелкой вверх — принимает GeoJSON |

Действия над объектом живут в карточке снизу, а не в подсказке про
горячие клавиши: про `F2` и `Delete` надо знать заранее, про кнопку — нет.
Пока фигура не набрала минимум точек, «Готово» погашена — это видно
до нажатия, а не выясняется им.

Рисование не мешает карте: клик ставит точку, а перетаскивание по-прежнему
двигает карту — они различаются по тому, сдвинулся ли курсор при нажатии.

Для линии показывается длина, для полигона — площадь и периметр, причём
прямо по ходу рисования. Считается по сфере, а не по плоскости Меркатора:
на широте 50° плоская формула завысила бы площадь более чем в полтора раза.

Объекты хранятся в `localStorage` браузера и переживают перезагрузку. При
первом запуске создаются **5 тестовых объектов** — метки «Харьков»,
«Тростянец» и «Ахтырка», линия и полигон. Чтобы вернуть их, очистите
объекты и перезагрузите страницу:

```javascript
localStorage.removeItem('mbtiles-viewer.objects')
```

Важно: хранилище привязано к браузеру и адресу. Объекты, созданные
на телефоне, на компьютере не появятся — для переноса используйте
экспорт и импорт GeoJSON.

Сами объекты векторные: они хранятся в градусах и перерисовываются на
каждом кадре, поэтому остаются резкими на любом зуме — в отличие
от растровой подложки, которая за пределами z18 просто растягивается.
Рисуются они на том же холсте, что и тайлы, сразу поверх них: отдельный
слой в DOM при зуме отставал бы от подложки на доли секунды.

## Доступ с телефона или планшета

Сервер слушает все интерфейсы, но Windows по умолчанию не пропускает
входящие соединения на порт 8080. Правило нужно создать один раз,
**в консоли, запущенной от имени администратора**:

```bash
netsh advfirewall firewall add rule name="mbtiles viewer 8080" dir=in action=allow protocol=TCP localport=8080
```

После этого открывайте на телефоне `http://192.168.1.139:8080/` (точный
адрес сервер печатает при запуске — берите тот, что из вашей Wi-Fi сети).

Правило открывает порт для всей локальной сети. Если это нежелательно,
удалить его можно так:

```bash
netsh advfirewall firewall delete rule name="mbtiles viewer 8080"
```

## Настройки

Задаются переменными окружения:

| Переменная | По умолчанию | Что делает |
|---|---|---|
| `MAPS_DIR` | `D:\карты` | Где лежат `.mbtiles` |
| `PORT` | `8080` | Порт |
| `HOST` | `0.0.0.0` | `127.0.0.1` — закрыть доступ извне |
| `WORKERS` | `5` | Потоков чтения с диска |
| `CACHE_MB` | `256` | Кэш тайлов в памяти сервера |
| `RESCAN_MS` | `60000` | Как часто искать докопированные файлы |

В PowerShell переменные задаются через `$env:` **до** запуска (синтаксис
`VAR=значение команда` из bash здесь не работает). Пример — запуск только
для этого компьютера и с большим кэшем:

```powershell
cd "D:\карты\viewer"
$env:HOST = "127.0.0.1"; $env:CACHE_MB = "512"; npm start
```

## Как это устроено

```
Браузер (canvas)                 Node                          Диск
────────────────                 ──────────────────            ──────────
app.js                           server.js
 ├ pan / zoom / pinch             ├ HTTP + keep-alive
 ├ кэш ImageBitmap (600 шт)       ├ маршрут по клетке z9
 ├ подмена тайлами-предками       ├ LRU 256 МБ
 ├ очередь по приоритету (6)      └ 5 воркеров ────────────> 18 × .mbtiles
 └ предзагрузка на простое                                   (readonly)
```

Тайлы читаются прямо из `.mbtiles` — ничего никуда не распаковывается.
50 ГБ в виде отдельных файлов означали бы миллионы мелких файлов, на
которых NTFS сильно деградирует.

### Почему именно так

Карты лежат на HDD, и это определяет всю конструкцию: одно случайное
чтение стоит около 10 мс, а на экран нужно порядка сорока тайлов.
Отсюда четыре меры, в порядке значимости:

1. **Подмена предками.** Пока настоящий тайл грузится, на его место
   рисуется соответствующий кусок уже загруженного тайла с меньшего зума,
   растянутый до нужного размера. Картинка есть всегда — сначала мыльная,
   через доли секунды резкая. Проверено: прыжок на три уровня зума при
   пустом кэше даёт 100% покрытия экрана в тот же кадр.
2. **Пул воркеров.** `better-sqlite3` синхронный, и в главном потоке
   каждое чтение блокировало бы сервер целиком. Пять потоков позволяют
   диску держать несколько запросов сразу и переупорядочивать их.
3. **Два уровня кэша.** LRU в памяти сервера плюс `immutable` в браузере:
   тайл с данными координатами не меняется никогда, так что просмотренный
   район перестаёт создавать и сетевой трафик, и обращения к диску.
4. **Маршрутизация без ввода-вывода.** Каждый файл — это полная пирамида
   под одной клеткой z9. Для любого тайла глубже z9 нужный файл находится
   сдвигом координат, без единого обращения к диску.

Замер на этой машине (200 случайных тайлов z17, 6 параллельно, все 18 файлов
на месте):

| | p50 | p95 | тайлов/с |
|---|---|---|---|
| холодное чтение с HDD | 177 мс | 408 мс | 32 |
| повторное из кэша | 1.4 мс | 4.3 мс | 3129 |

Цифра 32 тайла/с — это честная стоимость случайного чтения по всем 50 ГБ.
Один экран (около 40 тайлов) при полностью холодном кэше собирается больше
секунды. Столько же тайлов из кэша отдаются за десяток миллисекунд.

Разрыв в сто раз объясняет, почему подмена предками важнее всей остальной
оптимизации: пользователь не ждёт эту секунду, он сразу видит растянутую
картинку, которая дорезкается по мере подхода тайлов.

Учтите при собственных замерах: случайные тайлы — намеренно худший сценарий.
Реальный просмотр почти всегда идёт по соседним тайлам и по уже виденным
местам, где работают и кэш сервера, и страничный кэш ОС, и кэш браузера.

### Если захочется сильно быстрее

Единственный рычаг крупнее всего перечисленного — перенести данные с HDD
на SSD. Диск `E:` пуст и на нём 118 ГБ свободно; случайное чтение там
быстрее в десятки раз. Переезд не требует правок кода:

```powershell
cd "D:\карты\viewer"
$env:MAPS_DIR = "E:\карты"; npm start
```

## Пока идёт копирование

Сервер сам определяет недокопированные файлы (сравнивает размер на диске
с тем, что записан в заголовке SQLite) и пропускает их, а раз в минуту
проверяет заново и подключает готовые без перезапуска. В просмотрщике
при этом показывается предупреждение со списком.

## Инструменты

```bash
node tools/bench.js 17 200 6
```

Замер отклика: зум, количество тайлов, параллельность. Первый проход —
цена настоящего попадания на диск, второй — то же самое из кэша.

```bash
node tools/dump.js C:\temp\tiles 9/305/172 10/610/344
```

Выгружает указанные тайлы в файлы. Полезно, чтобы глазами проверить
ориентацию и стыковку уровней.

## Файлы

| Файл | Назначение |
|---|---|
| `server.js` | HTTP, кэш, диспетчер запросов к воркерам |
| `lib/db-open.js` | Открытие баз, PRAGMA, определение схемы и формата |
| `lib/index-builder.js` | Таблица маршрутизации и её кэш |
| `lib/tile-worker.js` | Поток чтения тайлов |
| `lib/lru.js` | LRU с бюджетом в байтах |
| `public/app.js` | Просмотрщик на canvas, без зависимостей |
| `public/objects.js` | Метки, линии, полигоны, замеры, GeoJSON |
| `public/style.css` | Оформление: светлое стекло поверх тёмного снимка |
| `start.js` | Пуск автономного комплекта: ищет карты, открывает браузер |
| `tools/build-bundle.ps1` | Сборка комплекта для машины без интернета |
| `.npmrc` | Отключает лишнюю пересборку нативного модуля при установке |
| `INSTALL.md` | Развёртывание с нуля на чистой машине |
| `cache/index.json` | Кэш индекса, создаётся автоматически |
